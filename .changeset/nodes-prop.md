---
'@fuzdev/mdz': minor
---

add an optional `nodes` prop to `Mdz` for rendering a pre-parsed `MdzNode` tree (skips `mdz_parse`); cap inline link/tag nesting so deeply nested `[`/`<tag>` render literal
